<%= form_for([@restaurant,@restaurant.comments.build]) do |f| %>
  <div class="form-group">
    <%= f.text_area :body, class:"form-control", id:'form-imput' %>
  </div>
  <div class="form-group mb-5">
    <%= f.submit 'Enviar' %>
  </div>
<% end %>