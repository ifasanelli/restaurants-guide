<div class="container">
  <h1 class="mt-5 mb-4">Editar Cozinha</h1>

  <div>
      <% if @cuisine.errors.any?%>
          <p>Você deve corrigir os erros para continuar:</p>

          <% @cuisine.errors.full_messages.each do |message|  %>
              <p><%= message %></p>
          <% end %>
      <% end %>
  </div>

  <%= form_with model: @cuisine do |f| %>
    <div class="form-group mb-4">
      <%= f.label :name, 'Nome:' %>
      <%= f.text_field :name, class:"form-control" %>
    </div>
    <div>
      <div class="form-group mb-4">
          <%= f.submit 'Salvar', class:"btn btn-danger" %>
      </div>
    </div>
  <% end %>

  <%= link_to 'Voltar', cuisine_path, class:"btn btn-outline-dark" %>
</div>